[build]
builder = "dockerfile"

[deploy]
startCommand = "streamlit run frontend-streamlit/streamlit_app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --server.enableCORS false --server.enableXsrfProtection false"
healthcheckPath = "/"
